#!/usr/bin/env sh
# Syncs repositories and downloads updates, meant to be run as a cronjob.

ping -q -c 1 1.1.1.1 >/dev/null || exit 1
sudo pacman -Syyuw --noconfirm
polybar-msg hook modules/updates 1
