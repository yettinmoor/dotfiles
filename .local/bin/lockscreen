#!/bin/sh
# Pauses everything and locks the screen. Bound to mod+x.
mpc pause
ls /tmp/mpvsoc* | xargs -r -I{} /bin/sh -c "echo '{ \"command\": [\"set_property\", \"pause\", true] }' | socat - {}" 2>/dev/null
i3lock -e -i ~/.config/lock.png
