# vim: filetype=sh

path() {
    echo $HOME/.local/bin
    echo $HOME/.cargo/bin
    echo $HOME/.nimble/bin
    echo $HOME/.ghcup/bin
    command -v go && go env GOBIN
    echo /usr/local/bin
}

PATH=$(path | /usr/bin/xargs -I {} sh -c '[ -d "{}" ] && echo "{}"' | tr '\n' ':')$PATH

unset -f path
