#!/bin/sh
# Shows clipboard selection as QR code. Mapped to mod+shift+Insert.

xclip -sel clip -o | qrencode -s 25 -o /tmp/qrclip.png || { notify-send "Empty clipboard."; exit 1; }
sxiv -pb -s h -N qrclip -g 725x725 /tmp/qrclip.png &
