#!/usr/bin/env sh

cd ~/docs/mnemo || exit 1

fd -e mnemo -X mnemo -i \
    | cut -d ' ' -f2 \
    | paste -sd '+' \
    | bc \
    | awk '{print $1, "cards due"}'

fd --strip-cwd-prefix -e mnemo -X mnemo -i
