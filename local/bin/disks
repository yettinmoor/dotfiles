#!/usr/bin/env sh
df -h \
    | awk \
        'BEGIN {
            print "DISK FREE USED%"
            print "---- ---- -----"
        }
        /^\/dev/{print $6, $4, $5}
    ' \
    | grep -v 'boot' \
    | tabulate
