#!/usr/bin/env sh

if [ "$1" = '-l' ]; then shift; lang="$1"; shift; fi

URL=https://en.wiktionary.org/api/rest_v1/page/definition

curl -s "$URL/$(if [ $# -eq 0 ]; then cat; else echo "$@"; fi | tr ' ' '_')" \
    | sed 's/<[^>]*>//g' \
    | jq ".$lang"
