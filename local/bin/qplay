#!/usr/bin/env sh
cd ~/music
sel="$(fd | sort | sed 's/^\.\///' | echo "$(cat -)" '\nSTOP' | rofi -dmenu -i -p 'play')" || exit 1
mpc clear
if ! [ "$sel" = 'STOP' ]; then
    echo "$sel" | mpc add
    mpc play
fi
