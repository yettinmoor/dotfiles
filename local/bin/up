#!/usr/bin/env sh

case "$1" in
    "down")
        ping -q -c 1 1.1.1.1 >/dev/null || exit 1
        sudo pacman -Syyuw --noconfirm
        vim +'PlugUpdate --sync' +qa
        ;;
    "")
        sudo pacman -Syu
        ;;
    *)
        exit 1
        ;;
esac

dwmbar updates
